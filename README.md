# updater-vscode
A script that manages VS Code updates and settings. Every Monday at 09:00 AM, it checks for new versions, verifies the download SHA-256, and merges 132 settings into all macOS user profiles while leaving personal themes and extensions alone.
